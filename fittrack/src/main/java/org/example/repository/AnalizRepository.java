package org.example.repository;

import org.example.model.Analiz;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AnalizRepository extends JpaRepository<Analiz, Long> {}
